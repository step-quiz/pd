# Notes internes (no publicat)

Carpeta `_dev/` — exclosa del desplegament via `.gitignore`.

- `BRIEFING-sessions-pendents.md` — briefing per generar els fitxers LaTeX de les sessions pendents.
- Aquesta carpeta no s'ha de servir mai en producció.
